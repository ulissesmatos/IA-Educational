<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="gradient-bg min-h-screen">
  
  <!-- Background Decoration -->
  <div style="position: fixed; inset: 0; overflow: hidden; pointer-events: none; z-index: 0;">
    <div style="position: absolute; top: -20%; left: -10%; width: 40%; height: 40%; background: radial-gradient(circle, rgba(37, 99, 235, 0.08) 0%, transparent 70%);"></div>
    <div style="position: absolute; bottom: -20%; right: -10%; width: 50%; height: 50%; background: radial-gradient(circle, rgba(59, 130, 246, 0.06) 0%, transparent 70%);"></div>
  </div>

  <div class="container container-lg py-8" style="position: relative; z-index: 1;">
    
    <!-- Back Button -->
    <a href="/" class="btn btn-secondary mb-6 animate-fadeInDown" style="display: inline-flex; align-items: center; gap: 8px;">
      <%- include('icons/arrow-left', { size: 18 }) %>
      Voltar ao início
    </a>
    
    <!-- Header -->
    <header class="text-center mb-8 animate-fadeInDown" style="animation-delay: 50ms;">
      <div class="animate-float" style="display: inline-flex; align-items: center; justify-content: center; width: 80px; height: 80px; background: linear-gradient(135deg, var(--accent-primary), var(--accent-dark)); border-radius: 20px; margin-bottom: 1.5rem; color: white;">
        <%- include('icons/puzzle-piece', { size: 48 }) %>
      </div>
      <h1 class="text-display gradient-text mb-2">IA ou Não?</h1>
      <p class="text-caption" style="max-width: 500px; margin: 0 auto;">
        Quiz interativo para testar se você consegue distinguir conteúdo criado por IA de conteúdo humano
      </p>
    </header>

    <!-- Main Cards Grid -->
    <div class="grid gap-6 mb-8 animate-fadeInUp" style="grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); max-width: 800px; margin: 0 auto; animation-delay: 100ms;">
      
      <!-- Card: Criar Sala (Para Professores/Facilitadores) -->
      <a href="/host" class="card card-hover" style="text-decoration: none; padding: 2rem;">
        <div class="flex items-start gap-4">
          <div style="display: flex; align-items: center; justify-content: center; width: 56px; height: 56px; background: var(--accent-glow); border-radius: 14px; flex-shrink: 0; color: var(--accent-primary);">
            <%- include('icons/tv', { size: 28 }) %>
          </div>
          <div style="flex: 1;">
            <div class="flex items-center gap-2 mb-2">
              <h2 class="text-title">Criar Sala</h2>
              <span class="badge badge-primary">HOST</span>
            </div>
            <p class="text-caption mb-3" style="color: var(--text-secondary);">
              Crie uma sala para apresentar o quiz na tela principal. Ideal para professores e facilitadores.
            </p>
            <div class="flex gap-2 flex-wrap">
              <span class="badge badge-neutral" style="font-size: 0.75rem; display: inline-flex; align-items: center; gap: 4px;">
                <%- include('icons/presentation-chart-bar', { size: 12 }) %> Apresentação
              </span>
              <span class="badge badge-neutral" style="font-size: 0.75rem; display: inline-flex; align-items: center; gap: 4px;">
                <%- include('icons/adjustments-horizontal', { size: 12 }) %> Controle total
              </span>
            </div>
          </div>
        </div>
        <div style="margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid var(--border-color);">
          <span class="text-small" style="color: var(--accent-primary); font-weight: 500; display: inline-flex; align-items: center; gap: 4px;">
            Criar nova sala <%- include('icons/arrow-right', { size: 14 }) %>
          </span>
        </div>
      </a>

      <!-- Card: Entrar em Sala (Para Alunos/Participantes) -->
      <a href="/join" class="card card-hover" style="text-decoration: none; padding: 2rem;">
        <div class="flex items-start gap-4">
          <div style="display: flex; align-items: center; justify-content: center; width: 56px; height: 56px; background: var(--accent-glow); border-radius: 14px; flex-shrink: 0; color: var(--accent-primary);">
            <%- include('icons/cursor-arrow-rays', { size: 28 }) %>
          </div>
          <div style="flex: 1;">
            <div class="flex items-center gap-2 mb-2">
              <h2 class="text-title">Entrar em Sala</h2>
              <span class="badge badge-success">JOGAR</span>
            </div>
            <p class="text-caption mb-3" style="color: var(--text-secondary);">
              Tem um código de sala? Entre aqui para participar do quiz em tempo real.
            </p>
            <div class="flex gap-2 flex-wrap">
              <span class="badge badge-neutral" style="font-size: 0.75rem; display: inline-flex; align-items: center; gap: 4px;">
                <%- include('icons/device-phone-mobile', { size: 12 }) %> Mobile friendly
              </span>
              <span class="badge badge-neutral" style="font-size: 0.75rem; display: inline-flex; align-items: center; gap: 4px;">
                <%- include('icons/bolt', { size: 12 }) %> Tempo real
              </span>
            </div>
          </div>
        </div>
        <div style="margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid var(--border-color);">
          <span class="text-small" style="color: var(--accent-primary); font-weight: 500; display: inline-flex; align-items: center; gap: 4px;">
            Inserir código <%- include('icons/arrow-right', { size: 14 }) %>
          </span>
        </div>
      </a>
    </div>

    <!-- How it Works Section -->
    <section class="animate-fadeInUp" style="animation-delay: 200ms; max-width: 800px; margin: 0 auto;">
      <h2 class="text-headline mb-6 text-center" style="display: flex; align-items: center; justify-content: center; gap: 8px;">
        <%- include('icons/question-mark-circle', { size: 24, className: 'icon-primary' }) %>
        Como funciona?
      </h2>
      
      <div class="grid gap-4" style="grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));">
        
        <!-- Step 1 -->
        <div class="card" style="padding: 1.5rem; text-align: center;">
          <div style="width: 48px; height: 48px; border-radius: 50%; background: var(--accent-glow); color: var(--accent-primary); display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-weight: 700; font-size: 1.25rem;">
            1
          </div>
          <h3 class="text-body" style="font-weight: 600; margin-bottom: 0.5rem;">Professor cria sala</h3>
          <p class="text-small" style="color: var(--text-muted);">
            O facilitador cria uma sala e projeta na tela
          </p>
        </div>
        
        <!-- Step 2 -->
        <div class="card" style="padding: 1.5rem; text-align: center;">
          <div style="width: 48px; height: 48px; border-radius: 50%; background: var(--accent-glow); color: var(--accent-primary); display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-weight: 700; font-size: 1.25rem;">
            2
          </div>
          <h3 class="text-body" style="font-weight: 600; margin-bottom: 0.5rem;">Alunos entram</h3>
          <p class="text-small" style="color: var(--text-muted);">
            Participantes escaneiam QR code ou digitam o código
          </p>
        </div>
        
        <!-- Step 3 -->
        <div class="card" style="padding: 1.5rem; text-align: center;">
          <div style="width: 48px; height: 48px; border-radius: 50%; background: var(--accent-glow); color: var(--accent-primary); display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-weight: 700; font-size: 1.25rem;">
            3
          </div>
          <h3 class="text-body" style="font-weight: 600; margin-bottom: 0.5rem;">Votam em tempo real</h3>
          <p class="text-small" style="color: var(--text-muted);">
            Cada um vota se o conteúdo foi feito por IA ou humano
          </p>
        </div>
        
        <!-- Step 4 -->
        <div class="card" style="padding: 1.5rem; text-align: center;">
          <div style="width: 48px; height: 48px; border-radius: 50%; background: var(--accent-glow); color: var(--accent-primary); display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-weight: 700; font-size: 1.25rem;">
            4
          </div>
          <h3 class="text-body" style="font-weight: 600; margin-bottom: 0.5rem;">Ranking final</h3>
          <p class="text-small" style="color: var(--text-muted);">
            Veja quem acertou mais no ranking final
          </p>
        </div>
      </div>
    </section>

    <!-- Content Types Section -->
    <section class="mt-8 animate-fadeInUp" style="animation-delay: 300ms; max-width: 800px; margin-left: auto; margin-right: auto;">
      <h2 class="text-headline mb-6 text-center" style="display: flex; align-items: center; justify-content: center; gap: 8px;">
        <%- include('icons/squares-2x2', { size: 24, className: 'icon-primary' }) %>
        Tipos de conteúdo
      </h2>
      
      <div class="card" style="padding: 1.5rem;">
        <div class="grid gap-4" style="grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));">
          
          <div class="flex items-center gap-3">
            <div style="width: 40px; height: 40px; border-radius: 10px; background: var(--bg-tertiary); display: flex; align-items: center; justify-content: center; color: var(--accent-primary);">
              <%- include('icons/photo', { size: 20 }) %>
            </div>
            <div>
              <span class="text-body" style="font-weight: 600;">Imagens</span>
              <p class="text-small" style="color: var(--text-muted);">Fotos e ilustrações</p>
            </div>
          </div>
          
          <div class="flex items-center gap-3">
            <div style="width: 40px; height: 40px; border-radius: 10px; background: var(--bg-tertiary); display: flex; align-items: center; justify-content: center; color: var(--accent-primary);">
              <%- include('icons/document-text', { size: 20 }) %>
            </div>
            <div>
              <span class="text-body" style="font-weight: 600;">Textos</span>
              <p class="text-small" style="color: var(--text-muted);">Artigos e redações</p>
            </div>
          </div>
          
          <div class="flex items-center gap-3">
            <div style="width: 40px; height: 40px; border-radius: 10px; background: var(--bg-tertiary); display: flex; align-items: center; justify-content: center; color: var(--accent-primary);">
              <%- include('icons/magnifying-glass', { size: 20 }) %>
            </div>
            <div>
              <span class="text-body" style="font-weight: 600;">Alucinações</span>
              <p class="text-small" style="color: var(--text-muted);">Fatos verdadeiros ou falsos</p>
            </div>
          </div>
          
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="mt-8 text-center animate-fadeInUp" style="animation-delay: 400ms;">
      <p class="text-small" style="color: var(--text-muted);">
        Desenvolvido para uso educacional
      </p>
    </footer>
    
  </div>
</body>
</html>
