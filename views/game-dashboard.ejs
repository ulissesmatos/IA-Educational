<!DOCTYPE html>
<html lang="pt-BR">
<%- include('partials/head', { title }) %>
<body class="gradient-bg min-h-screen">
  
  <%- include('components/ui/background-decoration') %>
  <%- include('partials/public-header', { currentPath: '/jogo' }) %>
  
  <main class="container container-lg" style="padding-top: 3rem; padding-bottom: 3rem; position: relative; z-index: 1;">
    
    <%- include('components/sections/hero-section', { 
      pageTitle: gameName,
      pageSubtitle: 'Quiz interativo para testar se você consegue distinguir conteúdo criado por IA de conteúdo humano',
      pageIcon: 'puzzle-piece'
    }) %>

    <!-- Action Cards -->
    <div class="action-cards-wrapper animate-fadeInUp" style="animation-delay: 100ms;">
      
      <a href="/host" class="action-card">
        <div class="action-card-top">
          <div class="card-icon-box">
            <%- include('icons/tv', { size: 32 }) %>
          </div>
          <div class="card-title-area">
            <h2>Criar Sala</h2>
            <span class="badge badge-primary">HOST</span>
          </div>
        </div>
        
        <p class="card-description">
          Crie uma sala para apresentar o quiz na tela principal. Ideal para professores e facilitadores.
        </p>
        
        <div class="card-tags">
          <span class="badge badge-neutral" style="font-size: 0.875rem;">
            <%- include('icons/presentation-chart-bar', { size: 14 }) %> Apresentação
          </span>
          <span class="badge badge-neutral" style="font-size: 0.875rem;">
            <%- include('icons/adjustments-horizontal', { size: 14 }) %> Controle total
          </span>
        </div>
        
        <div class="card-footer-link">
          Criar nova sala
          <%- include('icons/arrow-right', { size: 16 }) %>
        </div>
      </a>

      <a href="/join" class="action-card">
        <div class="action-card-top">
          <div class="card-icon-box">
            <%- include('icons/cursor-arrow-rays', { size: 32 }) %>
          </div>
          <div class="card-title-area">
            <h2>Entrar em Sala</h2>
            <span class="badge badge-success">JOGAR</span>
          </div>
        </div>
        
        <p class="card-description">
          Tem um código de sala? Entre aqui para participar do quiz em tempo real.
        </p>
        
        <div class="card-tags">
          <span class="badge badge-neutral" style="font-size: 0.875rem;">
            <%- include('icons/device-phone-mobile', { size: 14 }) %> Mobile friendly
          </span>
          <span class="badge badge-neutral" style="font-size: 0.875rem;">
            <%- include('icons/bolt', { size: 14 }) %> Tempo real
          </span>
        </div>
        
        <div class="card-footer-link">
          Inserir código
          <%- include('icons/arrow-right', { size: 16 }) %>
        </div>
      </a>
    </div>

    <!-- How it Works -->
    <section class="animate-fadeInUp" style="animation-delay: 200ms;">
      <h2 class="text-headline text-center" style="margin-bottom: 2.5rem;">
        <%- include('icons/question-mark-circle', { size: 28 }) %>
        Como funciona?
      </h2>
      
      <div class="info-grid">
        <div class="info-box">
          <div class="info-number">1</div>
          <h3>Professor cria sala</h3>
          <p>O facilitador cria uma sala e projeta na tela</p>
        </div>
        
        <div class="info-box">
          <div class="info-number">2</div>
          <h3>Alunos entram</h3>
          <p>Participantes escaneiam QR code ou digitam o código</p>
        </div>
        
        <div class="info-box">
          <div class="info-number">3</div>
          <h3>Votam em tempo real</h3>
          <p>Cada um vota se o conteúdo foi feito por IA ou humano</p>
        </div>
        
        <div class="info-box">
          <div class="info-number">4</div>
          <h3>Ranking final</h3>
          <p>Veja quem acertou mais no ranking final</p>
        </div>
      </div>
    </section>

    <!-- Content Types -->
    <section class="animate-fadeInUp" style="animation-delay: 300ms;">
      <h2 class="text-headline text-center" style="margin-bottom: 2.5rem;">
        <%- include('icons/squares-2x2', { size: 28 }) %>
        Tipos de conteúdo
      </h2>
      
      <div class="content-grid">
        <div class="content-item">
          <div class="content-icon-box">
            <%- include('icons/photo', { size: 28 }) %>
          </div>
          <div>
            <h4>Imagens</h4>
            <p>Fotos e ilustrações</p>
          </div>
        </div>
        
        <div class="content-item">
          <div class="content-icon-box">
            <%- include('icons/document-text', { size: 28 }) %>
          </div>
          <div>
            <h4>Textos</h4>
            <p>Artigos e redações</p>
          </div>
        </div>
        
        <div class="content-item">
          <div class="content-icon-box">
            <%- include('icons/magnifying-glass', { size: 28 }) %>
          </div>
          <div>
            <h4>Alucinações</h4>
            <p>Fatos verdadeiros ou falsos</p>
          </div>
        </div>
      </div>
    </section>

  </main>

  <%- include('partials/public-footer') %>

  <style>
    .action-cards-wrapper {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
      margin: 3rem 0 2rem;
    }

    .action-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 24px;
      padding: 2rem;
      text-decoration: none;
      color: inherit;
      display: flex;
      flex-direction: column;
      transition: all 0.3s ease;
      box-shadow: 0 4px 6px rgba(0,0,0,0.05);
    }

    .action-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px rgba(0,0,0,0.1);
      border-color: var(--accent-primary);
    }

    .action-card-top {
      display: flex;
      gap: 1.25rem;
      margin-bottom: 1.5rem;
    }

    .card-icon-box {
      width: 64px;
      height: 64px;
      background: var(--accent-glow);
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--accent-primary);
      flex-shrink: 0;
    }

    .card-title-area h2 {
      font-size: 1.75rem;
      font-weight: 700;
      margin: 0 0 0.5rem;
      color: var(--text-primary);
    }

    .card-description {
      color: var(--text-secondary);
      line-height: 1.6;
      margin-bottom: 1rem;
      font-size: 1rem;
    }

    .card-tags {
      display: flex;
      gap: 0.75rem;
      flex-wrap: wrap;
      margin-bottom: 1.5rem;
    }

    .card-footer-link {
      padding-top: 1.25rem;
      border-top: 1px solid var(--border-color);
      color: var(--accent-primary);
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-top: auto;
    }

    .info-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .info-box {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 16px;
      padding: 2rem 1.5rem;
      text-align: center;
    }

    .info-number {
      width: 56px;
      height: 56px;
      border-radius: 50%;
      background: var(--accent-glow);
      color: var(--accent-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.25rem;
      font-weight: 700;
      font-size: 1.5rem;
    }

    .info-box h3 {
      font-size: 1.125rem;
      font-weight: 600;
      margin: 0 0 0.75rem;
    }

    .info-box p {
      color: var(--text-secondary);
      margin: 0;
      line-height: 1.6;
    }

    .content-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 24px;
      padding: 2.5rem 2rem;
    }

    .content-item {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
    }

    .content-icon-box {
      width: 56px;
      height: 56px;
      background: var(--bg-tertiary);
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--accent-primary);
      flex-shrink: 0;
    }

    .content-item h4 {
      font-size: 1.125rem;
      font-weight: 600;
      margin: 0 0 0.25rem;
    }

    .content-item p {
      font-size: 0.9375rem;
      color: var(--text-secondary);
      margin: 0;
    }

    @media (max-width: 992px) {
      .info-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 768px) {
      .action-cards-wrapper {
        grid-template-columns: 1fr;
      }
      
      .info-grid {
        grid-template-columns: 1fr;
      }
      
      .content-grid {
        grid-template-columns: 1fr;
        padding: 2rem 1.5rem;
      }
    }
  </style>
</body>
</html>

