<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% var _siteName = (typeof siteName !== 'undefined' && siteName) ? siteName : (typeof locals !== 'undefined' && locals.siteName) ? locals.siteName : 'IA ou Não?'; %>
    <title><%= title || 'Admin' %> - <%= _siteName %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/admin/css/admin.css">
</head>
<body class="admin-body">
    <div class="admin-layout">
        <!-- Sidebar -->
        <aside class="admin-sidebar">
            <div class="admin-sidebar-header">
                <h1 class="admin-sidebar-title"><%= _siteName %></h1>
                <p class="admin-sidebar-subtitle">Painel Admin</p>
            </div>
            
            <nav class="admin-nav">
                <a href="/admin" class="admin-nav-link <%= currentPage === 'dashboard' ? 'active' : '' %>">
                    <svg class="admin-nav-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                    </svg>
                    Dashboard
                </a>
                
                <a href="/admin/quiz" class="admin-nav-link <%= currentPage === 'quiz' ? 'active' : '' %>">
                    <svg class="admin-nav-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    Perguntas Quiz
                </a>
                
                <a href="/admin/ai-tools" class="admin-nav-link <%= currentPage === 'ai-tools' ? 'active' : '' %>">
                    <svg class="admin-nav-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                    Ferramentas IA
                </a>
            </nav>
            
            <div class="admin-sidebar-footer">
                <div class="admin-user-info">
                    <div class="admin-user-avatar">
                        <%= adminName ? adminName.charAt(0).toUpperCase() : 'A' %>
                    </div>
                    <div class="admin-user-details">
                        <p class="admin-user-name"><%= adminName || 'Admin' %></p>
                        <p class="admin-user-email"><%= adminEmail || '' %></p>
                    </div>
                </div>
                
                <form action="/admin/logout" method="POST" style="margin-top: 1rem;">
                    <button type="submit" class="admin-btn admin-btn-secondary admin-btn-sm" style="width: 100%;">
                        Sair
                    </button>
                </form>
            </div>
        </aside>
        
        <!-- Main Content -->
        <main class="admin-main">
