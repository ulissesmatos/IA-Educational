<!DOCTYPE html>
<html lang="pt-BR">
<%- include('partials/head', { title }) %>
<body class="min-h-screen" style="background: #ffffff;">
  
  <%- include('components/ui/background-decoration') %>
  
  <main class="container container-lg" style="padding-top: 4rem; padding-bottom: 4rem; position: relative; z-index: 1;">
    
    <!-- Header -->
    <header class="text-center mb-8">
      <div class="flex items-center justify-center gap-3 mb-2">
        <span style="color: var(--accent-light);"><%- include('icons/cpu-chip', { size: 40 }) %></span>
        <h1 class="text-display gradient-text"><%= gameName %></h1>
      </div>
      <p class="text-caption">Painel do Facilitador</p>
    </header>

    <!-- Create Room Section -->
    <div class="animate-fadeInUp">
      <div class="card text-center" style="padding: 3rem 2rem; max-width: 480px; margin: 0 auto;">
        <div class="animate-float" style="margin-bottom: 1.5rem; color: var(--accent-light);">
          <%- include('icons/puzzle-piece', { size: 64 }) %>
        </div>
        <h2 class="text-headline mb-2">Criar Nova Sala</h2>
        <p class="text-secondary mb-6">Inicie uma nova sessão de quiz para seus participantes</p>
        
        <% if (typeof error !== 'undefined' && error) { %>
          <%- include('components/ui/callout', { 
            calloutType: 'error',
            calloutMessage: error
          }) %>
        <% } %>
        
        <form method="POST" action="/host" id="create-form">
          <button type="submit" id="btn-create" class="btn btn-primary btn-xl w-full">
            <span id="create-btn-text">Criar Sala</span>
            <svg id="create-spinner" class="hidden" style="width: 1.25rem; height: 1.25rem; animation: spin 1s linear infinite;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10" stroke-opacity="0.25"></circle>
              <path d="M12 2a10 10 0 0 1 10 10" stroke-linecap="round"></path>
            </svg>
          </button>
        </form>
        
        <p class="text-caption mt-6" style="color: var(--text-muted); display: flex; align-items: center; justify-content: center; gap: 0.5rem;">
          <%- include('icons/light-bulb', { size: 16 }) %> Após criar a sala, você receberá um código para compartilhar com os participantes
        </p>
      </div>
    </div>
  </main>

  <style>
    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
  </style>

  <script>
    document.getElementById('create-form').addEventListener('submit', function() {
      document.getElementById('btn-create').disabled = true;
      document.getElementById('create-btn-text').textContent = 'Criando sala...';
      document.getElementById('create-spinner').classList.remove('hidden');
    });
  </script>
</body>
</html>
